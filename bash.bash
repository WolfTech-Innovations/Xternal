# Three.js for 3D rendering
npm install three

# WebGazer.js for eye tracking
npm install webgazer

# Socket.IO for real-time communication (used for meeting hosting and joining)
npm install socket.io-client

# WebRTC for voice chat (no separate NPM package, but WebRTC is built into the browser API)
